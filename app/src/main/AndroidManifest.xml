<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.loader.module2module">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:name=".application.RouterApplication"
        android:supportsRtl="true"
        android:theme="@style/IMTheme.Transparent_Right">
        <activity android:name=".MainActivity"
            android:theme="@style/MainPageTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProgressActivity"/>
        <activity android:name=".TestAbsActivity"/>
        <activity android:name=".NetWorkActivity"/>
        <activity android:name=".NetMasterActivity"/>

        <!--视频播放-->
        <activity android:name=".media.ActivityListView"/>

        <!--屏幕适配-->
        <activity android:name=".AutoSizeActivity"/>
       <!-- 全局设计图尺寸 360 * 640-->
        <!-- 如果您项目中的所有页面都只需要以高或宽中的一个作为基准进行适配的话, 那就只需要填写高或宽中的一个设计图尺寸即可 -->
        <meta-data
            android:name="design_width_in_dp"
            android:value="360"/>
        <meta-data
            android:name="design_height_in_dp"
            android:value="640"/>

    </application>

</manifest>